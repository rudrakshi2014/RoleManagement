= form_for @role, html: { class: "form form-horizontal role" } do |f|
  .form-group
    .col-lg-offset-2.col-lg-10
      = f.submit nil,class: 'btn btn-primary'
      = link_to t('.cancel', :default => t("helpers.links.cancel")), roles_path, :class => 'btn btn-default'
